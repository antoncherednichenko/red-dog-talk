I have refactored the socket events and types according to the new documentation.

1. **Updated** **`lib/constants/socket.ts`**:

   * Changed event names to colon-separated format (`room:join`, `room:leave`, `member:update_status`, `member:status_changed`).

2. **Updated** **`lib/api/rooms.ts`**:

   * Updated `ROOM_MEMBER_STATUS` to include `AFK` and remove `AWAY`/`BUSY`.

3. **Refactored** **`lib/hooks/use-room-events.ts`**:

   * Updated types to match the new documentation.

   * Updated `socket.emit` calls to use new event names.

   * Updated `socket.on` handler to expect full `RoomMember` object.

4. **Updated** **`components/providers/room-provider.tsx`**:

   * Updated `handleMemberStatusChanged` to accept `IRoomMemberDTO` (full member object).

   * Updated toast notifications to use `payload.user.name` directly.

   * Improved state update logic to handle both updates and new member additions.

